<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>KU은행 - 지출 관리</title>
  <link rel="stylesheet" href="/css/style.css">
  <style>
    body { margin: 0; font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif; }

    header {
        display: flex;
        justify-content: space-between;
        align-items: center;
        padding: 15px 40px;
        background-color: #ffffff;
        border-bottom: 2px solid #036b3f;
        box-shadow: 0 4px 10px rgba(0,0,0,0.05);
    }
    
    header h1 a {
        display: flex;
        align-items: center;
        text-decoration: none;
        gap: 12px;
    }

    .logo-img {
        height: 40px;
        width: auto;
    }

    .logo-text {
        font-size: 1.6rem;
        font-weight: 800;
        color: #036b3f;
        letter-spacing: -1px;
    }

    .nav-menu {
        display: flex;
        gap: 70px; 
    }
    .nav-menu a {
        text-decoration: none;
        color: #444;
        font-weight: 600;
        font-size: 1.5rem;
        padding: 10px 18px;
        border-radius: 25px;
        background-color: transparent; 
        transition: all 0.2s ease-in-out; 
    }
    
    .nav-menu a:hover {
        background-color: rgba(3, 107, 63, 0.08); 
        color: #036b3f;
    }

    .user-menu {
        display: flex;
        align-items: center;
        gap: 15px;
    }
    .logout-btn {
        font-size: 0.9rem;
        color: #555;
        text-decoration: none;
        border: 1px solid #ddd;
        padding: 6px 14px;
        border-radius: 4px;
        transition: background 0.2s;
    }
    .logout-btn:hover {
        background-color: #f8f9fa;
        color: #d6336c;
        border-color: #d6336c;
    }
    
    .auth-link {
        color: #555;
        text-decoration: none;
        font-weight: 600;
        font-size: 0.95rem;
    }
    .auth-link:hover {
        color: #036b3f;
        text-decoration: underline;
    }
  </style>
</head>
<body>
  <header>
    <h1>
        <a href="/">
            <img src="/images/logo.png" alt="KU Logo" class="logo-img">
            <span class="logo-text">KU은행</span>
        </a>
    </h1>

    <nav class="nav-menu">
      <% if (locals.user) { %>
        <a href="/expenses">지출 내역</a>
        <a href="/expenses/ranking">소비 랭킹</a>
      <% } %>
    </nav>

    <div class="user-menu">
      <% if (locals.user) { %>
        <span style="color: #444; font-size: 0.95rem;">
            안녕하세요, <strong><%= user.username %></strong>님
        </span>
        <a href="/users/logout" class="logout-btn">로그아웃</a>
        <form action="/users/withdraw?_method=DELETE" method="POST" style="margin: 0;" 
            onsubmit="return confirm('정말 탈퇴하시겠습니까?\n작성한 모든 지출 내역이 영구적으로 삭제됩니다.');">
          <button type="submit" style="
              background: none; 
              border: none; 
              color: #999; 
              font-size: 0.85rem; 
              text-decoration: underline; 
              cursor: pointer; 
              padding: 0;
              margin-left: 5px;
          " onmouseover="this.style.color='#dc3545'" onmouseout="this.style.color='#999'">
              회원탈퇴
          </button>
        </form>
        <% } else { %>
          <a href="/users/login" class="auth-link">로그인</a>
          <span style="color: #ddd;">|</span>
          <a href="/users/register" class="auth-link">회원가입</a>
        <% } %>
      </div>
  </header>