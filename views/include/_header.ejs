<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>지출 관리 대시보드</title>
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  <header>
    <h1>
        <a href="/">
          <img src="/images/logo.png" alt="KU Logo" class="logo-img">
          <span class="logo-text">KU은행</span>
        </a>
    </h1>
    
    <nav class="nav-menu">
        <% if (locals.user) { %>
            <% if (locals.user.role !== 'admin') { %>
                <a href="/expenses">지출 내역</a>
                <a href="/expenses/ranking">소비 랭킹</a>
            <% } else { %>
                <a href="/admin" style="color: #dc3545;">공지사항</a>
            <% } %>
            <a href="/users/mypage">마이페이지</a>
        <% } %>
    </nav>

    <div class="user-menu">
      <% if (locals.user) { %>
        <span style="color: #444; font-size: 0.95rem;">
            안녕하세요, <strong><%= user.username %></strong>님
        </span>

        <a href="/users/logout" class="user-btn" style="color: #d6336c; border-color: #f8d7da;">로그아웃</a>

        <% } else { %>
          <a href="/users/login" class="auth-link">로그인</a>
          <span style="color: #ddd;">|</span>
          <a href="/users/register" class="auth-link">회원가입</a>
        <% } %>
      </div>
  </header>