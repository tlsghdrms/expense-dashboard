<%- include('./include/_header') %>

<div class="container ranking-container"> 
    <h2 class="ranking-title">🏆 <%= month %>월 소비 리포트</h2>
    <div class="ranking-card">
        <div class="ranking-deco-bar"></div>
        <h3 class="ranking-username">
            <strong><%= user.username %></strong> 님의 순위
        </h3>

        <div class="ranking-number">
            <%= myRank %><span class="ranking-unit">위</span>
        </div>
        <p class="ranking-total-users">( 전체 <%= totalUsers %>명 중 )</p>

        <div class="ranking-amount-box">
            <p class="ranking-amount-label">이번 달 총 지출액</p>
            <p class="ranking-amount-value">
                <%= myTotal.toLocaleString() %> <span class="ranking-amount-unit">원</span>
            </p>
        </div>

        <p class="ranking-message">
            <% if (percentile <= 20) { %>
                <span class="msg-success">🎉 축하합니다! 상위 <%= percentile %>% 절약왕!</span>
            <% } else if (percentile >= 80) { %>
                <span class="msg-danger">🚨 주의! 상위 <%= percentile %>% 과소비 상태입니다.</span>
            <% } else { %>
                <span class="msg-neutral">🙂 상위 <%= percentile %>% 로 평균적인 소비 중입니다.</span>
            <% } %>
        </p>
    </div>
    
    <div class="ranking-btn-area">
        <a href="/expenses" class="btn btn-primary btn-ranking-back">
            나의 지출로 돌아가기
        </a>
    </div>
</div>

<%- include('./include/_footer') %>